<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Lead_Validar_Email</fullName>
    <active>true</active>
    <description>Garante formato de e-mail válido para a Connecta Solutions.</description>
    <errorConditionFormula>AND(
   NOT(ISBLANK(Email)),
   NOT(REGEX(Email, &quot;^[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,4}$&quot;))
)</errorConditionFormula>
    <errorDisplayField>Email</errorDisplayField>
    <errorMessage>O e-mail digitado é inválido. Use o formato: nome@dominio.com</errorMessage>
</ValidationRule>
